"use strict";
function makeShirt(size, color, message) {
    console.log(`You have ordered a ${size}  ${color} T-shirt with the message: "${message}"`);
    return;
}
//Call the function
makeShirt(`Large`, `Pink`, `Type your text here!!.`);
